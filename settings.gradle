dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // settings maven thư viện Mchart

        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "QLTCDA1"
include ':app'
